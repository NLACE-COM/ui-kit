---
category: Forms
---
Toggle accesible (role="switch"), controlado o no controlado, con label y descripción opcionales.

`<Switch label="Notificaciones" description="Recibir alertas por email" defaultChecked />`

- Controlado con `checked` + `onChange(checked)`, o no controlado con `defaultChecked`.
- `size`: `sm` | `md`. `disabled` atenúa e inhabilita.
