---
category: Charts
---
Variante de LineChart con relleno degradado bajo la línea.

```jsx
<AreaChart labels={['Q1','Q2','Q3','Q4']} series={[{ name: 'Ingresos', values: [120, 150, 140, 190] }]} />
```

- Misma API que `LineChart` (sin necesidad de `area`). `showDots`, `legend`, `height`.
